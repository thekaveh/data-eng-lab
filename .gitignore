__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/
.DS_Store
# Claude Code / superpowers scratch (brainstorm mockups, SDD reports — never committed)
.superpowers/
# MkDocs built site
site/
# wiki staging dir (built by scripts/build_docs.py, never committed)
wiki/
# local data (never committed)
datasets/raw/
datasets/generated/
**/data/
**/runs/
# preflight/report artifacts
.preflight/
# Maven build output
target/
# never commit any env file with secrets
.env
*.env
