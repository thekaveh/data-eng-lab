# Changelog

All notable changes to this project are documented here (Keep a Changelog format).

## [Unreleased]
### Added
- Phase 0: repository foundation, Atlas submodule, launch harness, base tooling,
  verifier skeleton, and infra-preflight Layer 1.
- Docs overhaul: three self-contained synced surfaces (in-repo READMEs, GitHub
  Wiki, .io MkDocs site) generated from `docs/` via `scripts/build_docs.py`;
  Orbital theme; 19 auto-extracted notebook docs; 23 landscape SVG diagrams;
  CI gates (`check_surfaces`, `check_diagrams`, `build_docs --check`).
